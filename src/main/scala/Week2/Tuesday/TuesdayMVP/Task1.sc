// 1. What are three main differences between a class and a case class?
//- class can be extended, case class can't (it's the lowest level of the tree)
// - case classes can only use immutable data
//- methods are built in in case classes, e.g. apply method, (no need to use keyword 'new')